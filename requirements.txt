Flask>=3.1.0
Flask-SQLAlchemy>=3.1.1
SQLAlchemy>=2.0.36
python-dotenv>=1.0.1
Werkzeug>=3.1.3
gunicorn>=23.0.0

# Postgres / Supabase (psycopg v3 has reliable prebuilt wheels; unpinned so
# pip can pick a build with wheels for whatever Python version you're on —
# exact old pins like 3.2.3 have no wheel yet for very new Python releases)
psycopg[binary]>=3.2.3

# Groq AI SDK
groq>=1.5.0

# File extraction
PyMuPDF>=1.25.1
python-docx>=1.1.2
python-pptx>=1.0.2

# Optional image OCR support (needs system tesseract-ocr binary too)
pytesseract>=0.3.13
opencv-python-headless>=4.10.0.84
Pillow>=11.0.0

# PDF report generation
fpdf2>=2.8.2
